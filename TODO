TODO List

* If repoman see a dependence on AUR which isn't on own repository ask
to pull that dependence on own repository.

* A way to get packages list from $repo.db.tar.gz

* add --ignore packages options
